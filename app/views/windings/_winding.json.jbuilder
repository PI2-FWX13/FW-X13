json.extract! winding, :project_name, :id, :length, :radius, :offset, :filament_width, :filament_length, :gelPot, :density, :layers, :angle, :winding_date, :created_at, :updated_at
json.url winding_url(winding, format: :json)
